version: '3'
services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: blog
      POSTGRES_PASSWORD: meau-de-pace
      POSTGRES_USER: paul
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
    driver: local
